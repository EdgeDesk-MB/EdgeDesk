# Polish backlog

Small UX tweaks harvested from real daily use - not roadmap features, not §9 ideas.
Sam batches these up and hands them over "for the right occasion"; fix in sweeps.

| # | Observed | Fix direction | Status |
|---|----------|---------------|--------|
| P1 | Tapping an "offer expiring with EV unclaimed" push notification lands on the Add bet flow instead of showing the offer | The `offer_expiring` alert rule uses the Do next item's href (`rules.ts:100`, tracker/add-bet oriented); it should deep-link to the offer campaign details modal instead (needs an `/offers?view=<offerId>` style param that opens `viewOffer` on load) | ✅ Done 2026-07-15 — rule emits the deep link, offers page opens `viewOffer` once and strips the param. Param renamed `view` → `offer` on 2026-09-26 (EDGE-159) because the public demo owns `?view=`; `?view=<id>` is still read for notifications already sent |
| P2 | Mobile quick menu only offered bet capture | Quick actions grid added to the quick-log sheet: New offer, Add balance, Matched calc, Casino offer, Track fixture — every global modal reachable without navigation | ✅ Done 2026-07-15 (Sam's request) |
| P3 | Casino page had no help entry | `casino` PageHelpId + header `?` wired; site map already lists Casino via NAV_SECTIONS | ✅ Done 2026-07-15 |
| P4 | Dutching/Accumulator/Rule4 still looked like generic admin tables while every other calculator had moved to the bold MBB coloured-panel idiom; Dutching only supported "Total stake" entry; **dutch bets could only be created via the calculator handoff, and editing an existing one silently dropped its legs** (Add bet's edit-hydrate never read `editBet.legs`, so saving corrupted it into a plain single bet) | Shared `DutchOutcomesBuilder` (`src/components/calc/dutch-outcomes-builder.tsx`) mounted by both the Dutching calculator and a new "Dutch" entry in Add bet's Bet type dropdown - one component, so behaviour can't drift. Three stake modes: Total stake (existing), Target profit (`dutchStakeForProfit`, already in the calc layer, now wired to the UI), First outcome (new `dutchStakeForLegStake`, tests-first). Add bet's edit-hydrate now restores `editBet.legs`, PATCH gained `legs` support, and save() PATCHes instead of always POSTing when editing - a dutch bet now round-trips through edit without corruption (harness-verified: create → edit → change a leg's odds → save, legs and type intact throughout). Accumulator and Rule 4 restyled onto `BackPanel`/`PanelInput`/`PanelSelect`; the 2UP dutch tab's windfall highlight swapped a light-mode-only hardcoded colour for the `success` token | ✅ Done 2026-07-20 |
| P5 | Dutch legs had no venue at all (bookmaker was a hidden, never-set field) and no way to apply a free bet the way every other bet type can | Each leg now carries its own `bookmaker` (`VenueSelect`, bookie or exchange) plus an optional `freeBet: "snr" \| "sr"` flag, exclusive to whichever leg is fixed in "First outcome" mode. New calc function `dutchStakesForFreeLeg` (tests-first) solves the cash legs so every outcome pays the free leg's win-profit exactly, given the free stake is real money only if it loses is impossible (it's already sunk). `settleBet`'s dutch branch applies free_snr/free_sr profit rules per leg. `ledgerBetPlacement` debits the free leg's stake from its venue's tracked free-bet balance at creation; a new `reledgerDutchFreeLegs` re-syncs that debit on PATCH (calc-auditor caught that edits bypass `ledgerBetPlacement` entirely - fixed before commit). Edit-hydrate restores the free-bet leg, its venue and its original stake (previously ANY dutch edit silently reset to Total-stake-£100 on reopen, an existing but newly-consequential gap since it would have also silently un-free'd a free leg - fixed as part of this). Harness-verified: venue picker, free-bet toggle + "Use available", save → ledger debit → edit round-trip → PATCH reconciliation (add/re-stake/remove a free leg all correctly adjust the tracked balance) | ✅ Done 2026-07-20 |

**Known limitation from P5, not yet fixed:** retention/season-report/bookmaker-stats/edge-report all identify a "free bet" by `bet.betType === "free_snr" \| "free_sr"` - a dutch bet with one free leg keeps `betType: "dutch"`, so none of those aggregations count it. The account balance is correctly debited either way; only free-bet usage *reporting* undercounts dutch legs. Needs its own pass through each consumer if Sam wants that closed.
