# Implementation brief

Read `.cursor/skills/brief/SKILL.md` and follow it. In short: produce a self-contained work order in the `edgeways/docs/roadmap/implementation-briefs.md` format, sized exactly one of `[local]` / `[strong]` / `[design-first]`, with data shapes verified against current code and cross-brief dependencies called out. Update the "Last updated" line when appending.
