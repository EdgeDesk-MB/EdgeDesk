# Iterative development

Read `.cursor/skills/iterative-dev/SKILL.md` and follow `docs/cursor-workflow.md`
exactly.

Classify first (calc / UI / hosted write / feed/store / schema/state /
docs/roadmap / mechanical). Announce files, approach, and
`Ticket: EDGE-n|none`. Load only the matching skill plus one source of
truth. Then smallest verifiable increment.

If you are not in the Cursor IDE (Orca, CLI, cloud agent), slash commands
will not fire for later steps. `Read` the matching `SKILL.md` yourself.
